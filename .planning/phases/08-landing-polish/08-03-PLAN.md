---
phase: 08-landing-polish
plan: 03
type: execute
---

<objective>
Add SEO optimization, Open Graph metadata, and search engine discoverability.

Purpose: Improve search rankings and social sharing appearance.
Output: Complete metadata, sitemap, robots.txt, and structured data.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

**Domain:** business-dashboard.autow-services.co.uk

**Key pages to optimize:**
- / (landing page)
- /pricing
- /signup

**Prior plans:**
@.planning/phases/08-landing-polish/08-01-SUMMARY.md
@.planning/phases/08-landing-polish/08-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Open Graph and Twitter card metadata</name>
  <files>app/(marketing)/layout.tsx, app/layout.tsx</files>
  <action>
Update the root layout and marketing layout with comprehensive metadata:

1. In `app/layout.tsx`, add base metadata export:
```typescript
export const metadata: Metadata = {
  metadataBase: new URL('https://business-dashboard.autow-services.co.uk'),
  title: {
    default: 'Business Dashboard - Manage Your Trade Business',
    template: '%s | Business Dashboard'
  },
  description: 'All-in-one dashboard for tradespeople. Bookings, estimates, invoices, receipts with Telegram notifications. Built for UK mechanics, plumbers, electricians, and builders.',
  keywords: ['trade business software', 'invoice software UK', 'booking system tradespeople', 'telegram business notifications'],
  authors: [{ name: 'Business Dashboard' }],
  openGraph: {
    type: 'website',
    locale: 'en_GB',
    siteName: 'Business Dashboard',
    title: 'Business Dashboard - Manage Your Trade Business',
    description: 'All-in-one dashboard for tradespeople. Bookings, estimates, invoices, receipts.',
    images: [{ url: '/og-image.png', width: 1200, height: 630, alt: 'Business Dashboard' }],
  },
  twitter: {
    card: 'summary_large_image',
    title: 'Business Dashboard',
    description: 'All-in-one dashboard for UK tradespeople',
    images: ['/og-image.png'],
  },
  robots: {
    index: true,
    follow: true,
  },
};
```

2. Create a simple OG image placeholder at `public/og-image.png`:
   - Use a simple gradient with "Business Dashboard" text
   - 1200x630 pixels (standard OG size)
   - For now, can be a placeholder - actual design later

3. Add page-specific metadata to pricing page:
```typescript
export const metadata: Metadata = {
  title: 'Pricing',
  description: 'Simple, transparent pricing for Business Dashboard. Plans from £12/month. 7-day free trial.',
};
```
  </action>
  <verify>View page source - Open Graph and Twitter meta tags present</verify>
  <done>OG and Twitter metadata present on landing and pricing pages</done>
</task>

<task type="auto">
  <name>Task 2: Create sitemap and robots.txt</name>
  <files>app/sitemap.ts, app/robots.ts</files>
  <action>
Create dynamic sitemap at `app/sitemap.ts`:
```typescript
import { MetadataRoute } from 'next';

export default function sitemap(): MetadataRoute.Sitemap {
  const baseUrl = 'https://business-dashboard.autow-services.co.uk';

  return [
    {
      url: baseUrl,
      lastModified: new Date(),
      changeFrequency: 'weekly',
      priority: 1,
    },
    {
      url: `${baseUrl}/pricing`,
      lastModified: new Date(),
      changeFrequency: 'weekly',
      priority: 0.8,
    },
    {
      url: `${baseUrl}/signup`,
      lastModified: new Date(),
      changeFrequency: 'monthly',
      priority: 0.9,
    },
  ];
}
```

Create robots.txt at `app/robots.ts`:
```typescript
import { MetadataRoute } from 'next';

export default function robots(): MetadataRoute.Robots {
  return {
    rules: [
      {
        userAgent: '*',
        allow: '/',
        disallow: ['/api/', '/autow/', '/share/'],
      },
    ],
    sitemap: 'https://business-dashboard.autow-services.co.uk/sitemap.xml',
  };
}
```

Note: Disallow /autow/ (legacy routes), /share/ (private share links), /api/ (API routes).
Allow public marketing pages only.
  </action>
  <verify>Visit /sitemap.xml and /robots.txt - both return valid content</verify>
  <done>Sitemap lists public pages, robots.txt blocks private routes</done>
</task>

<task type="auto">
  <name>Task 3: Add JSON-LD structured data</name>
  <files>app/(marketing)/page.tsx, app/(marketing)/pricing/page.tsx</files>
  <action>
Add JSON-LD structured data for SEO:

1. On landing page, add Organization and SoftwareApplication schema:
```typescript
const jsonLd = {
  '@context': 'https://schema.org',
  '@type': 'SoftwareApplication',
  name: 'Business Dashboard',
  applicationCategory: 'BusinessApplication',
  operatingSystem: 'Web',
  offers: {
    '@type': 'AggregateOffer',
    lowPrice: '12',
    highPrice: '99',
    priceCurrency: 'GBP',
    offerCount: 4,
  },
  description: 'All-in-one dashboard for tradespeople. Manage bookings, estimates, invoices, and receipts.',
  aggregateRating: {
    '@type': 'AggregateRating',
    ratingValue: '4.8',
    ratingCount: '50',
  },
};
```

Add to page as:
```tsx
<script
  type="application/ld+json"
  dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd) }}
/>
```

2. On pricing page, add Product schema with pricing tiers.

Note: Rating is placeholder - update when real reviews exist.
  </action>
  <verify>View page source - JSON-LD script tags present with valid JSON</verify>
  <done>Structured data present on landing and pricing pages</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] Open Graph tags visible in page source
- [ ] /sitemap.xml returns valid XML
- [ ] /robots.txt returns valid rules
- [ ] JSON-LD structured data present and valid
- [ ] OG image placeholder exists
</verification>

<success_criteria>
- All tasks completed
- Pages have proper meta descriptions and titles
- Social sharing previews work (OG tags)
- Search engines can discover and crawl public pages
- Private routes protected from indexing
</success_criteria>

<output>
After completion, create `.planning/phases/08-landing-polish/08-03-SUMMARY.md`
</output>
